<%= form_with url: video_path, method: :get do |form| %>
  <%= form.label :yt_url, "Enter YouTube URL: " %>
  <%= form.text_field :yt_url %>
  <%= form.submit "Describe" %>
<% end %>
<%= flash[:notice] %>
<h1 class="page-title">Recently searched videos</h1>

<table class="table table-striped col-md-12" id="movies">
  <thead>
    <tr>
      <th>Title</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @videos_info.each do |video| %>
      <tr>
        <td>
          <%= video["title"] %>
        </td>
        <td>
          <%= link_to "Show", "/video/#{video["id"]}", :class => 'btn btn-primary', data: { turbolinks: 'false' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>